import styled from "styled-components";

const Wrapper = styled.main`
  height: 100vh;
  position: relative;

  .main-section {
    display: grid;
    gap: 1rem;
    padding: 1rem;
  }
`;

export default Wrapper;
