import styled from "styled-components";

const Wrapper = styled.main`
  height: 100vh;

  .main-section {
    display: grid;
    gap: 1rem;
    padding: 1rem;
  }

  .section-cards {
    display: grid;
    gap: 1rem;
  }
`;

export default Wrapper;
